{
  "_comment": "模型路由配置点（唯一人工编辑源）。provider 名与 ZCode 里配置的 provider 一致（如“基元”），model 用 ZCode 里列出的模型 id。coder/reviewer 由子代理钉模型执行：~/.zcode/agents/coder-z.md、reviewer-z.md 的 model 字段由 scripts/sync_agents.py 从本文件生成，不要手改。主会话保持 roles.planner 模型即可。换模型：改这里 → 跑 scripts/sync_agents.py → scripts/validate.py 校验。",
  "roles": {
    "planner": { "provider": "基元", "model": "deepseek-v4-flash-0731" },
    "coder": { "provider": "基元", "model": "deepseek-v4-flash" },
    "reviewer": { "provider": "基元", "model": "deepseek-v4-flash" }
  },
  "execution": {
    "driver": "subagent",
    "max_task_retries": 3
  }
}
